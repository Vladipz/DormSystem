# Frontend Notes

Frontend setup and repository-wide onboarding now live in the root `README.md`.

Use that file as the primary entry point for running and understanding the project.

Useful local commands from this directory:

```bash
npm install
npm run dev
npm run build
npm run lint
npm run typecheck
```
